=== D:/Claude/tcc_svi_vol/01_coleta_dados.ipynb ===
  [md 0] # Coleta de Dados

Coleta e preparo da base de dados do TCC.

- **Etapa 1:** preços e volatilidade realizada (Yahoo Finance)
- **Etapa 2:** SVI semanal por ticker (Google Trends)
- **Etapa 3:** teste 
  [code 1] import time import math import warnings warnings.filterwarnings("ignore")  import pandas as pd import numpy as np import yfinance as yf  from pytrends.request import TrendReq from datetime import date
  [code 2] # ────────────────────────────────────────────────────────────── # CONFIGURAÇÕES # ────────────────────────────────────────────────────────────── ANOS_HISTORICO    = 5 VOLUME_MIN_DIARIO = 1 SEMANAS   
  [md 3] ## Dados financeiros
  [code 4] # ────────────────────────────────────────────────────────────── # ETAPA 1 — PREÇOS # ──────────────────────────────────────────────────────────────  # Todos os ticker tickers = pd.read_csv("./data/ac
  [code 5] # ────────────────────────────────────────────────────────────── # ETAPA 2 — VOLATILIDADE REALIZADA # ──────────────────────────────────────────────────────────────  series_semanais = [] for _, row in
  [md 6] ## GOOGLE TRENDS: SVI POR TICKER
  [code 7] # ────────────────────────────────────────────────────────────── # ETAPA 3 — GOOGLE TRENDS: SVI POR TICKER # ──────────────────────────────────────────────────────────────  pytrends = TrendReq(hl="pt-
  [md 8] ## ESTACIONARIEDADE DO SVI (ADF POR TICKER)
  [code 9] # ────────────────────────────────────────────────────────────── # ETAPA 4 — ESTACIONARIEDADE DO SVI (ADF POR TICKER) # ────────────────────────────────────────────────────────────── from statsmodels.
  [md 10] ## Base de dados final
  [code 11] # ────────────────────────────────────────────────────────────── # ETAPA 5 — MERGE: PREÇOS + TRENDS # Uma linha por (semana, ticker) # ──────────────────────────────────────────────────────────────  d

=== D:/Claude/tcc_svi_vol/02_modelos.ipynb ===
  [md 0] # Modelos e Avaliação

Especificação, estimação e avaliação out-of-sample dos modelos competidores.

| Modelo | Especificação |
|--------|---------------|
| M0 — Random Walk | σ²ₜ = RV_{t-1} |
| M1 — 
  [code 1] import warnings warnings.filterwarnings("ignore")  import pandas as pd import numpy as np import matplotlib.pyplot as plt  from arch import arch_model from arch.bootstrap import MCS  CAMINHO = "./data
  [md 2] # Bases salvas
  [code 3] df_acoes = pd.read_csv("./data/acoes_elegiveis.csv") df_trends = pd.read_csv("./data/google_trends_svi.csv", parse_dates=["semana"]) df_precos = pd.read_csv("./data/precos_semanais.csv", parse_dates=[
  [md 4] # Especificação dos Modelos Competidores
  [md 5] ## MODELO 0: PASSEIO ALEATÓRIO (Random Walk)
σ̂²_t = RV_{t-1}
  [code 6] def passeio_aleatorio(grupo):     df_t = (         grupo.sort_values("semana")         .reset_index(drop=True)         [["semana", "rv"]]         .copy()     )     df_t["rv_pred_m0"] = df_t["rv"].shif
  [md 7] ## MODELO 1: GARCH(1,1) PADRÃO
σ²_t = ω + α·ε²_{t-1} + β·σ²_{t-1}

Janela móvel de tamanho fixo: 52 * 3 semanas
  [code 8] def garch_padrao(grupo, col_end="ret_semanal", janela=52 * 3):     df_t = (         grupo.sort_values("semana")         .reset_index(drop=True)         [["semana", "rv", col_end]]         .copy()     
  [md 9] ## MODELO 2: GARCH-X(1,1)
$(σ²_t = ω + α·ε²_{t-1} + β·σ²_{t-1} + γ·ΔSVI_{t-1})$

Janela móvel de tamanho fixo: 52 * 3 semanas

---
O que muda em relação ao Modelo 1:

A única diferença estrutural é o 
  [code 10] def garch_exog(grupo, col_end="ret_semanal", col_exog="svi_diff", janela=52 * 3):     df_t = (         grupo.sort_values("semana")         .reset_index(drop=True)         [["semana", "rv", col_end, co
  [md 11] # Avaliação de Previsão e Inferência (Modelos Aninhados)
```
Janela de treino:   svi_diff[ t-156 … t-1 ]  →  estima γ
Previsão:           svi_diff[ t-1 ]           →  usa γ para prever σ²_t


  [code 12] # ── Proporção de zeros no SVI por ticker ───────────────────────────── # Calculada aqui para uso nos três cenários de avaliação. # O filtro NÃO é aplicado sobre df_final — todos os tickers entram # n
  [code 13] # ── Configurações ───────────────────────────────────────────────── JANELA = 52 * 3  NOMES = {     "qlike_m0": "M0 (RW)",     "qlike_m1": "M1 (GARCH)",     "qlike_m2": "M2 (GARCH-X)", }
  [code 14] # ── Loop principal ───────────────────────────────────────────────── resultados, resumo_params = [], []  for ticker, grupo in df_final.groupby("ticker_b3"):     print(f"► {ticker}", end=" ... ")     
  [code 15] # Preserva versão completa (usada na comparação diagnóstica) ticker_erro_m2 = df_previsoes[df_previsoes.status_m2 == "Singular matrix"]["ticker_b3"].unique() df_previsoes_raw = df_previsoes.copy()  # 
  [md 16] # Diagnóstico: Comparação de Estratégias de Filtro

São testadas quatro estratégias e comparados: número de tickers rodados,
tickers com erro (singular matrix), tickers na comparação final, QLIKE e MC
  [code 17] # ── Função de perda QLIKE ─────────────────────────────────────── def qlike(rv, pred):     mask = (rv > 0) & (pred > 0)     r    = np.where(mask, rv / pred, np.nan)     return np.where(mask, r - np.l
  [code 18] TXT = CAMINHO + "diagnostico_filtros.txt" open(TXT, "w", encoding="utf-8").close()  # limpa arquivo  df_mcs_1, qlike_1 = comparar_cenario(     df_previsoes_raw,     threshold_zeros=None, remove_singul

=== D:/Claude/tcc_svi_vol/03_analise_resultados.ipynb ===
  [md 0] # Análise dos Resultados

Análise exploratória com filtro **≥50% de zeros** no SVI:
remove tickers com 50% ou mais das semanas com SVI = 0.

Requer `previsoes_consolidadas.csv` gerado por `02_modelos.
  [code 1] import warnings warnings.filterwarnings("ignore")  import pandas as pd import numpy as np import matplotlib.pyplot as plt import seaborn as sns import yfinance as yf  from arch.bootstrap import MCS  C
  [code 2] # ── Carrega dados ──────────────────────────────────────────────── df_previsoes    = pd.read_csv(CAMINHO + "previsoes_consolidadas.csv", parse_dates=["semana"]) df_acoes        = pd.read_csv(CAMINHO 
  [code 3] df_qlike["melhor"] = df_qlike[list(NOMES.values())].idxmin(axis=1) print(df_qlike["melhor"].value_counts())  # Cruzar γ-significância com qual modelo vence por ticker tickers_signif = df_resumo_params
  [code 4] # ── Grupos ───────────────────────────────────────────────────── tickers_m2_vence = df_qlike[df_qlike["melhor"] == "M2 (GARCH-X)"].index tickers_m2_perde = df_qlike[df_qlike["melhor"] != "M2 (GARCH-X
  [code 5] print(df_analise[df_analise["vol_bucket"] == ">500M"][["grupo", "vol_fin_medio"]].sort_values("vol_fin_medio", ascending=False)) print(df_analise.groupby("grupo")["zeros_pct"].median())
  [code 6] # Verificar PDGR3 no df_acoes print(df_acoes[df_acoes["ticker_yf"] == "PDGR3.SA"])  # E no df_final para ver o volume semanal print(df_final[df_final["ticker_b3"] == "PDGR3"][["semana", "volume_total"
  [code 7] import matplotlib.pyplot as plt  # % de tickers com svi=0 por semana, separado por grupo df_final["grupo"] = np.where(     df_final["ticker_b3"].isin(tickers_m2_vence), "M2 vence", "M2 perde" )  zeros

